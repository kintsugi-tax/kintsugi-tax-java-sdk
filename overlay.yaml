overlay: 1.0.0
x-speakeasy-jsonpath: rfc9535
info:
  title: Kintsugi Tax Platform API Overlay - Minimal Java SDK
  version: 1.0.0
actions:
  # =============================================================================
  # MINIMAL JAVA SDK - ONLY ESSENTIAL ENDPOINTS
  # =============================================================================
  # Start with just a few endpoints to see if we can get the generator working
  # =============================================================================

  # === ONLY ADDRESS VALIDATION - MINIMAL TEST ===
  - target: $.paths['/v1/address_validation/search'].post
    update:
      x-public: true

  # =============================================================================
  # REMOVE EVERYTHING ELSE
  # =============================================================================
  - target: $.paths.*[?@['x-public'] != true]
    remove: true

# =============================================================================
# MINIMAL JAVA SDK TEST
# =============================================================================
# This overlay includes only one endpoint to test if the Java generator
# can handle the most basic case without compilation errors
# =============================================================================