/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.kintsugi.taxplatform.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.kintsugi.taxplatform.utils.Utils;
import java.lang.Override;
import java.lang.String;
import java.time.LocalDate;
import org.openapitools.jackson.nullable.JsonNullable;


public class OssRegistrationCountryRead {
    /**
     * The unique identifier for the OSS registration country.
     */
    @JsonProperty("id")
    private String id;


    @JsonProperty("country_code")
    private CountryCodeEnum countryCode;

    /**
     * The date from which this country is officially covered by the OSS registration.
     */
    @JsonProperty("effective_date")
    private LocalDate effectiveDate;

    /**
     * The date this country ceased to be covered by the OSS registration (if applicable).
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("end_date")
    private JsonNullable<LocalDate> endDate;

    /**
     * Status of a country within an OSS registration.
     * 
     * <p>Keep the members in sync with ``PublicOssCountryStatusEnum``: the tenanted API
     * projects this onto that public enum by value (``_to_public_oss_country`` in
     * ``registrations/routers/latest.py``), so a member added here without a matching
     * public member raises ``ValueError`` and 500s the tenanted read for any row carrying
     * the new status. Add it to both, or map it deliberately at the projection.
     */
    @JsonProperty("status")
    private OssCountryStatusEnum status;

    @JsonCreator
    public OssRegistrationCountryRead(
            @JsonProperty("id") String id,
            @JsonProperty("country_code") CountryCodeEnum countryCode,
            @JsonProperty("effective_date") LocalDate effectiveDate,
            @JsonProperty("end_date") JsonNullable<LocalDate> endDate,
            @JsonProperty("status") OssCountryStatusEnum status) {
        Utils.checkNotNull(id, "id");
        Utils.checkNotNull(countryCode, "countryCode");
        Utils.checkNotNull(effectiveDate, "effectiveDate");
        Utils.checkNotNull(endDate, "endDate");
        Utils.checkNotNull(status, "status");
        this.id = id;
        this.countryCode = countryCode;
        this.effectiveDate = effectiveDate;
        this.endDate = endDate;
        this.status = status;
    }
    
    public OssRegistrationCountryRead(
            String id,
            CountryCodeEnum countryCode,
            LocalDate effectiveDate,
            OssCountryStatusEnum status) {
        this(id, countryCode, effectiveDate,
            JsonNullable.undefined(), status);
    }

    /**
     * The unique identifier for the OSS registration country.
     */
    @JsonIgnore
    public String id() {
        return id;
    }

    @JsonIgnore
    public CountryCodeEnum countryCode() {
        return countryCode;
    }

    /**
     * The date from which this country is officially covered by the OSS registration.
     */
    @JsonIgnore
    public LocalDate effectiveDate() {
        return effectiveDate;
    }

    /**
     * The date this country ceased to be covered by the OSS registration (if applicable).
     */
    @JsonIgnore
    public JsonNullable<LocalDate> endDate() {
        return endDate;
    }

    /**
     * Status of a country within an OSS registration.
     * 
     * <p>Keep the members in sync with ``PublicOssCountryStatusEnum``: the tenanted API
     * projects this onto that public enum by value (``_to_public_oss_country`` in
     * ``registrations/routers/latest.py``), so a member added here without a matching
     * public member raises ``ValueError`` and 500s the tenanted read for any row carrying
     * the new status. Add it to both, or map it deliberately at the projection.
     */
    @JsonIgnore
    public OssCountryStatusEnum status() {
        return status;
    }

    public static Builder builder() {
        return new Builder();
    }


    /**
     * The unique identifier for the OSS registration country.
     */
    public OssRegistrationCountryRead withId(String id) {
        Utils.checkNotNull(id, "id");
        this.id = id;
        return this;
    }

    public OssRegistrationCountryRead withCountryCode(CountryCodeEnum countryCode) {
        Utils.checkNotNull(countryCode, "countryCode");
        this.countryCode = countryCode;
        return this;
    }

    /**
     * The date from which this country is officially covered by the OSS registration.
     */
    public OssRegistrationCountryRead withEffectiveDate(LocalDate effectiveDate) {
        Utils.checkNotNull(effectiveDate, "effectiveDate");
        this.effectiveDate = effectiveDate;
        return this;
    }

    /**
     * The date this country ceased to be covered by the OSS registration (if applicable).
     */
    public OssRegistrationCountryRead withEndDate(LocalDate endDate) {
        Utils.checkNotNull(endDate, "endDate");
        this.endDate = JsonNullable.of(endDate);
        return this;
    }

    /**
     * The date this country ceased to be covered by the OSS registration (if applicable).
     */
    public OssRegistrationCountryRead withEndDate(JsonNullable<LocalDate> endDate) {
        Utils.checkNotNull(endDate, "endDate");
        this.endDate = endDate;
        return this;
    }

    /**
     * Status of a country within an OSS registration.
     * 
     * <p>Keep the members in sync with ``PublicOssCountryStatusEnum``: the tenanted API
     * projects this onto that public enum by value (``_to_public_oss_country`` in
     * ``registrations/routers/latest.py``), so a member added here without a matching
     * public member raises ``ValueError`` and 500s the tenanted read for any row carrying
     * the new status. Add it to both, or map it deliberately at the projection.
     */
    public OssRegistrationCountryRead withStatus(OssCountryStatusEnum status) {
        Utils.checkNotNull(status, "status");
        this.status = status;
        return this;
    }

    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        OssRegistrationCountryRead other = (OssRegistrationCountryRead) o;
        return 
            Utils.enhancedDeepEquals(this.id, other.id) &&
            Utils.enhancedDeepEquals(this.countryCode, other.countryCode) &&
            Utils.enhancedDeepEquals(this.effectiveDate, other.effectiveDate) &&
            Utils.enhancedDeepEquals(this.endDate, other.endDate) &&
            Utils.enhancedDeepEquals(this.status, other.status);
    }
    
    @Override
    public int hashCode() {
        return Utils.enhancedHash(
            id, countryCode, effectiveDate,
            endDate, status);
    }
    
    @Override
    public String toString() {
        return Utils.toString(OssRegistrationCountryRead.class,
                "id", id,
                "countryCode", countryCode,
                "effectiveDate", effectiveDate,
                "endDate", endDate,
                "status", status);
    }

    @SuppressWarnings("UnusedReturnValue")
    public final static class Builder {

        private String id;

        private CountryCodeEnum countryCode;

        private LocalDate effectiveDate;

        private JsonNullable<LocalDate> endDate = JsonNullable.undefined();

        private OssCountryStatusEnum status;

        private Builder() {
          // force use of static builder() method
        }


        /**
         * The unique identifier for the OSS registration country.
         */
        public Builder id(String id) {
            Utils.checkNotNull(id, "id");
            this.id = id;
            return this;
        }


        public Builder countryCode(CountryCodeEnum countryCode) {
            Utils.checkNotNull(countryCode, "countryCode");
            this.countryCode = countryCode;
            return this;
        }


        /**
         * The date from which this country is officially covered by the OSS registration.
         */
        public Builder effectiveDate(LocalDate effectiveDate) {
            Utils.checkNotNull(effectiveDate, "effectiveDate");
            this.effectiveDate = effectiveDate;
            return this;
        }


        /**
         * The date this country ceased to be covered by the OSS registration (if applicable).
         */
        public Builder endDate(LocalDate endDate) {
            Utils.checkNotNull(endDate, "endDate");
            this.endDate = JsonNullable.of(endDate);
            return this;
        }

        /**
         * The date this country ceased to be covered by the OSS registration (if applicable).
         */
        public Builder endDate(JsonNullable<LocalDate> endDate) {
            Utils.checkNotNull(endDate, "endDate");
            this.endDate = endDate;
            return this;
        }


        /**
         * Status of a country within an OSS registration.
         * 
         * <p>Keep the members in sync with ``PublicOssCountryStatusEnum``: the tenanted API
         * projects this onto that public enum by value (``_to_public_oss_country`` in
         * ``registrations/routers/latest.py``), so a member added here without a matching
         * public member raises ``ValueError`` and 500s the tenanted read for any row carrying
         * the new status. Add it to both, or map it deliberately at the projection.
         */
        public Builder status(OssCountryStatusEnum status) {
            Utils.checkNotNull(status, "status");
            this.status = status;
            return this;
        }

        public OssRegistrationCountryRead build() {

            return new OssRegistrationCountryRead(
                id, countryCode, effectiveDate,
                endDate, status);
        }

    }
}
